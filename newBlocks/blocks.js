// 5feadefbb01255000880e22a     Std
// 5feade65b01255000880e17e     UtilsUSL
// 5feadedeb01255000880e1f7     WidgetReactors
// 5fec19bf71ef3e041cf10538     DynamicObjectLibrary
// 5feadfa4b01255000880e2a8     DeviceUSL




function AddDialogButton(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].addDialogButton(value);
    return undefined;
}

function AddItem(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].addItem(value);
    return undefined;
}

function AddItemTree(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].addItemTree(value);
    return undefined;
}

function AddMessage(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].addMessage(value);
    return undefined;
}

function AddTab(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].addTab(value);
    return undefined;
}

function AppendContact(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].AppendContact(value);
    return undefined;
}

function GetBranchStyle(targetId) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].getBranchStyle();
    return undefined;
}

function GetCurrentItem(targetId) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].getCurrentItem();
    return undefined;
}

function GetCurrentTabButtonName(targetId) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].getCurrentTabButtonName();
    return undefined;
}

function GetCurrentTabWidgetName(targetId) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].getCurrentTabWidgetName();
    return undefined;
}

function GetHoverStyle(targetId) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].getHoverStyle();
    return undefined;
}

function GetItem(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].getItem(value);
    return undefined;
}

function GetItemIdAt(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].getItemIdAt(value);
    return undefined;
}

function GetItemText(targetId) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].getItemText();
    return undefined;
}

function GetLabelName(targetId) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].getLabelName();
    return undefined;
}

function GetLeafStyle(targetId) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].getLeafStyle();
    return undefined;
}

function GetNumTabs(targetId) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].getNumTabs();
    return undefined;
}

function GetRootItem(targetId) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].getRootItem();
    return undefined;
}

function GetSelectItem(targetId) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].getSelectItem();
    return undefined;
}

function GetSelectStyle(targetId) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].getSelectStyle();
    return undefined;
}

function GetSizeItems(targetId) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].getSizeItems();
    return undefined;
}

function GetStyleType(targetId) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].getStyleType();
    return undefined;
}

function GetUserName(targetId) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].getUserName();
    return undefined;
}

function IsChecked(targetId) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].isChecked();
    return undefined;
}

function IsValid(targetId) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].isValid();
    return undefined;
}

// function OnChangeChecked(targetId, value) {
//     if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID];
//     return undefined;
// }

// function OnChangeSelectTreeItem(targetId, value) {
//     if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID];
//     return undefined;
// }

// function OnLoaded(targetId, value) {
//     if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID];
//     return undefined;
// }

// function OnMouseUpContact(targetId, value) {
//     if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID];
//     return undefined;
// }

// function OnPressDialogButton(targetId, value) {
//     if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID];
//     return undefined;
// }

function RemoveDialogButton(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].removeDialogButton(value);
    return undefined;
}

function RemoveTab(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].removeTab(value);
    return undefined;
}

function SetAutoClose(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].setAutoClose(value);
    return undefined;
}

function SetAutoFontSize(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].setAutoFontSize(value);
    return undefined;
}

function SetCenterWidget(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].setCenterWidget(value);
    return undefined;
}

function SetChecked(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].setChecked(value);
    return undefined;
}

function SetColorContainer(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].setColorContainer(value);
    return undefined;
}

function SetCurrentItem(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].setCurrentItem(value);
    return undefined;
}

function SetCurrentTab(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].setCurrentTab(value);
    return undefined;
}

function SetEnableHeader(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].setEnableHeader(value);
    return undefined;
}

function SetExpandItem(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].setExpandItem(value);
    return undefined;
}

function SetLeftContainerWidth(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].setLeftContainerWidth(value);
    return undefined;
}

function SetLeftWidget(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].setLeftWidget(value);
    return undefined;
}

function SetLineVisible(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].setLineVisible(value);
    return undefined;
}

function SetLinkData(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].setLinkData(value);
    return undefined;
}

function SetMaxWidgetHeight(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].setMaxWidgetHeight(value);
    return undefined;
}

function SetMaxWidgetWidth(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].setMaxWidgetWidth(value);
    return undefined;
}

function SetMinWidgetHeight(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].setMinWidgetHeight(value);
    return undefined;
}

function SetMinWidgetWidth(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].setMinWidgetWidth(value);
    return undefined;
}

function SetRegularExp(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].setRegularExp(value);
    return undefined;
}

function SetSelectItemTree(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].setSelectItemTree(value);
    return undefined;
}

function SetShowRightPanel(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].setShowRightPanel(value);
    return undefined;
}

function SetSource(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].setSource(value);
    return undefined;
}

function SetSpacing(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].setSpacing(value);
    return undefined;
}

function SetStyleType(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].setStyleType(value);
    return undefined;
}

function SetTypeInput(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].setTypeInput(value);
    return undefined;
}

function SetTypeValidate(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].setTypeValidate(value);
    return undefined;
}

function StartLoading(targetId) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].startLoading();
    return undefined;
}

function StopLoading(targetId) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].stopLoading();
    return undefined;
}

function SetIconCollapce(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].setIconCollapce(value);
    return undefined;
}

function SetIconColor(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].setIconColor(value);
    return undefined;
}

function SetIconExpand(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].setIconExpand(value);
    return undefined;
}

function SetBackgroundColor(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].setBackgroundColor(value);
    return undefined;
}

function SetBorderSize(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].setBorderSize(value);
    return undefined;
}

function SetTextColor(targetId, value) {
    if (Rex && Rex.widgets[targetId]) return Rex.widgets[targetID].setTextColor(value);
    return undefined;
}
